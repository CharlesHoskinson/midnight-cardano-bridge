# Repository Instructions

This repository uses OpenSpec for specification-driven changes. Codex must use
the repository skills under `.codex/skills/openspec-*` for OpenSpec proposal,
exploration, application, synchronization, and archive work.

Treat the project context and artifact rules in `openspec/config.yaml` as
mandatory. Validate OpenSpec artifacts with `npm run openspec:validate` before
committing them.
